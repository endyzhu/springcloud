spring:
  application:
    name: baseinfo
  cloud:
    config:
      label: master
      name: baseinfo-dev
      profile: dev
      discovery:
        enabled: true
        serviceId: config-server

server:
  port: 7890
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1111/eureka/